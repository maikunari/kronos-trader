# Core
pyyaml>=6.0
python-dotenv>=1.0
numpy>=1.24
pandas>=2.0
requests>=2.31
websocket-client>=1.6

# Hyperliquid
hyperliquid-python-sdk>=0.9

# Indicators (Donchian, Keltner, ADX, Bollinger, ATR; SuperTrend + Hurst rolled in-repo)
ta>=0.11.0

# Backtest / reporting
matplotlib>=3.7
scipy>=1.11

# Utilities
aiohttp>=3.9
pyarrow>=14.0     # parquet support for candle caching in strategy_agent

# LLM strategy agent
anthropic>=0.40

# Dashboard (optional; not required for headless backtests or live loop)
streamlit>=1.30

# Test
pytest>=8.0
pytest-cov>=5.0
